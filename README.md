# ndev-install-script

From: https://archive.org/details/ndevdriverinstallerwin1011

Added dpinst.xml language check and patch if necessary so it doesn't silently exit if the proper lanugage code isn't found that corresponds to the user's default system language.
